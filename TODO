- Can we combine the foot/noise layout into a single entry so a cycle picks the single entry with
  both layouts, instead of cycling both?
  Or, can we have more abstract maps where we map footpedal1/pop to a generic action which can be
  gamepad, keyboard/mouse, or other user defined actions?

- Add additional noises (parrot?)
  https://gist.github.com/ghouston/609b771716c846029c8504ac70fd1de8

- Add words or phrases for certain games that will take actions?
  e.g. up = dpad_up
       regex = load regex into clipboard
- Have certain commands available when certain games are running?

- Experiment with eye tracking
