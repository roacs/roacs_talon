- Can we combine the foot/noise layout into a single entry so a cycle picks the single entry with
  both layouts, instead of cycling both?

- Add additional noises (parrot?)

- Add words or phrases for certain games that will take actions?
  e.g. up = dpad_up

- Experiment with eye tracking
